// Only change code below this line
function isLess(a, b) {

// Oly change code above this line
if (a < b) {
    // Only 
return true;
    // Only
} else {
    // Only 
return false;
    // Only
}
// only
}
console.log(isLess(10, 15));
console.log(isLess(15, 10));

// Only
module.exports = isLess;